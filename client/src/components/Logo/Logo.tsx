import logo from "/logo-get-pet.svg";

const Logo = () => {
  return (
    <div className="flex flex-row items-center justify-center">
      <img src={logo} alt="" />
    </div>
  );
};

export default Logo;
